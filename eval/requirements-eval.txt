# 生产评测依赖（独立于 experiments 最小依赖）
# 建议在 venv 中：pip install -r eval/requirements-eval.txt

requests>=2.28.0
evalscope[perf]>=0.16.0

# 能力评测（套系 B）额外依赖，体积较大
# pip install 'evalscope[opencompass]'  # 如需 OpenCompass 后端
# 基础能力评测通常 evalscope 本体即可
